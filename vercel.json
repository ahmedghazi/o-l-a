{
  "buildCommand": "cd web && pnpm install && pnpm generate",
  "outputDirectory": "web/.output/public",
  "installCommand": "npm install -g pnpm@9 && cd web && pnpm install",
  "framework": "nuxtjs"
}
